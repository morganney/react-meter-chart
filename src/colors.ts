interface Colors {
  dot?: string
  bounds?: string
  range?: string
  label?: string
}

const defaultColors = {
  dot: '#000000',
  bounds: '#eaeaea',
  range: '#70c47e66',
  label: '#000000',
}

export { defaultColors }
export type { Colors }
